# GalSGrammar —— BGalS 剧本脚本语言规范（v2）

**BGalS（Blessing Galgame Script）** 是 Blessing Galgame Engine（布雷辛格混合视觉小说引擎）的剧本脚本语言。设计初衷是让剧本编写规范化、标准化，并与引擎实现彻底解耦：编剧只需编写纯文本剧本、配置资源索引，无需接触引擎代码即可运行一部视觉小说。

> 本文档以代码实现为准，逐条核对自：
> `Autoloads/dialogue_importer.gd`（解析）、`Scenes/GalEventItem/instruction.gd`（指令枚举与参数）、`Autoloads/story_manager.gd`（运行时）、`Autoloads/dialogue_renderer.gd`（插值与锚点）。
> 若文档与代码冲突，以代码为准。

## 设计原则

- **BGalS 是叙事 DSL，不是通用语言。** 不提供脚本内 label/goto、循环、函数调用与表达式求值器；复杂玩法逻辑通过 `scene mount` 挂载 GDScript 玩法场景实现（逃逸口），结果写回变量供条件分支读取。
- **一行一个语句**，逐行解析；块结构由缩进表达，无任何收尾标记。
- **错误早报**：解析器对未定义指令、非法参数、缩进违规、结构不配对全部在编译期输出行号级诊断，不做静默容错。

## 设计理念：对话窗口与三时机模型

视觉小说的核心是对话文本：每次对话显示对应一次用户交互。BGalS 的流式执行以对话为中心，指令按与对话的**绑定方向**分三种时机：

```text
[对话A 显示+打字] → 打字完成
    → [> 后指令] → [独立指令]        ← 打字完成窗口，不等点击
    → 等待用户交互（点击/AUTO/SKIP）
    → [< 前指令] → [对话B 显示]       ← 推进窗口
```

- **前指令（`<`）**：跟随**下一句**对话——等待用户点击推进才执行（玩家停留在上一句期间不执行）。
- **后指令（`>`）**：跟随**上一句**对话——打字完成立即执行，不等点击。
- **独立指令（无前缀）**：不绑定任何对话——不等待用户交互，执行流到达即执行。书写位置决定具体时机：写在后指令区随打字完成执行，写在前指令区随点击推进执行。`var`、`wait`、不绑定对话的 `bg`/`music` 是独立指令的典型用例。
- AUTO 模式的推进时机为 **max(打字完成, 语音播完) + `auto_wait_time`**（BGalS v2.2 起），语音不再被自动播放切断；SKIP 不受此约束。

## 剧本的编译与加载

- 文本剧本放在 `config.json` 中 `scripts.read_dir` 指定的目录（默认 `scripts/`），引擎启动时由 `DialogueImporter` 编译为 Godot 资源，输出到 `scripts.save_dir`（默认 `GalSs/`）。
- 输出文件名为**相对路径去掉扩展名、路径分隔符替换为下划线**：`scripts/demo/scene1.txt` → `GalSs/demo_scene1.tres`，其**脚本名**为 `demo_scene1`。`jump` 等指令引用的就是这个名字。
- 编译按 SHA256 哈希增量进行（哈希记录于 `GalSs/.hash`），并混入**编译器版本盐**：编译器换代时全量重编，防止旧产物的枚举序列化错位。
- 产物带 `compiler` 版本标记，`ResourceManager` 加载时校验——`scripts.check` 置 `false` 的纯运行模式（模组包）下，陈旧产物会被拒绝加载而不是静默错位。
- `bg`/`music`/`sfx`/`voice`/`char` 的资源引用在编译期对照 `index.json` 登记表校验（含文本内联锚点），未登记给出**警告**而不是错误——模组 PCK 可提供登记表之外的资源。`scene mount` 的 `res://` 直路径不在此列。
- **剧本名不得为 `main_menu`**（与 `jump main_menu` 特殊目标冲突），编译期报错。

## 行规则

BGalS 逐行解析，一行一个语句。行分类按以下优先级判定（先到先匹配）：

1. **空行**、以 `//` 或 `#` 开头 → 注释（忽略，不打断块结构）。
2. 行首 `\` → **转义**，整行强制按对话行（去掉 `\`）。用于写出以保留字、`*`、`>`、`<` 开头的文本。
3. `>` 开头 → 后指令。
4. `<` 开头 → 前指令。
5. `*` 开头 → 选项行。
6. 首个词命中指令保留字表 → 独立指令。
7. 其余 → 对话行。

指令行、选项行、条件行支持行尾 `//` 行内注释（对话行中的 `//` 是普通文本）。

**保留字劫持防护**：保留字开头的旁白会被判定为指令（如 `wait 一下`）。此时参数解析必然失败，编译期报错并提示转义写法。请养成习惯：以保留字开头的文本行，行首加 `\`。

保留字表：`bg` `music` `sfx` `voice` `char` `var` `if` `elif` `else` `jump` `begin` `wait` `scene` `trans`。

## 缩进规则

- 一级缩进 = 4 空格；Tab 视为一级缩进；全角空格报错。
- 缩进加深只能跟在块头行（`*`、`if`、`elif`、`else`）之后，否则报错。
- 缩进深度必须是 4 的整数倍，否则报错。
- `if`/`elif`/`else` 块头后必须紧跟非空缩进体（空块报错）；选项允许空体（直接汇合）。
- 缩进回退即块结束——无 `endif` 等任何收尾标记。

## 对话行

```text
说话者: 对话内容
```

- 半角冒号 `:` 与全角冒号 `：` 都作为分隔符，按**第一个**冒号切分。
- 不含任何冒号的行视为**旁白**（无说话者）。注意旁白行不能包含任何冒号。
- 对话正文经 `RichTextLabel` 显示，**支持 BBCode 标签**；打字机效果把 `[...]` 标签整体瞬时插入，不会逐字拆坏标签。
- 转义：`\[` 输出字面量 `[`，`\{` 输出字面量 `{`，`\\` 输出字面量 `\`。

### 变量插值

```text
引路人: 当前好感度：{affection}。
```

- `{变量名}` 在显示时求值（运行时，跟随当前变量表）；未定义变量按 `0` 显示（与条件判断同口径）。
- 整数值省略小数部分（显示 `3` 而非 `3.0`）。转义 `\{`。

### 内联锚点

```text
引路人: 立绘可以移动[char 0 texture demo_char_b]、换图、淡入淡出。
引路人: 这句话中间[pause 0.5]会停顿一下。
```

- `[指令 参数]` 在打字机播放到该文本位置时触发指令。
- 白名单：`sfx`、`voice`、`char`、`bg`、`music`、`trans`；`[pause 秒]` 为打字机停顿（由对话 UI 内部消费）。
- 判定规则：`[` 后跟白名单指令名 + 定界符（空格或 `]`）才解析为锚点，否则原样视为 BBCode。锚点优先于同名自定义 BBCode 标签。
- 流程指令（`jump`/`begin`/`if`/`var`/`wait`/`scene` 等）不在白名单，天然拒绝。

## 指令参考

参数规则：**主参数位置化，修饰参数一律 `key:value` 键值对**；布尔值 `true`/`1`/`on` 为真。坐标写作 `x,y`（逗号后**不得有空格**）。条件表达式的比较符两侧空格可选，比较符仅支持 `==` `!=` `>` `>=` `<` `<=`（`==`/`!=` 为浮点近似比较）。未登记的修饰键在编译期给出警告（防止拼写错误静默失效）。

### 音频

| 指令 | 参数（默认值） | 说明 |
| --- | --- | --- |
| `music <引用>` | `[from:秒=0]` `[loop:bool=true]` `[fade:秒=1.0]` `[fade_in:秒]` `[fade_out:秒]` `[volume:0~1=1.0]` | 播放 BGM（双播放器交叉淡变；loop 为流内循环，无缝）。`fade:` 是双侧简写，`fade_in:`/`fade_out:` 显式覆盖对应侧（如 `fade_out:0.2 fade_in:2` 快出慢进）。 |
| `music stop` | `[fade:秒=1.0]` | 停止（淡出）。 |
| `music pause` / `music resume` | 无 | 暂停 / 恢复（作用于所有在播音轨）。 |
| `music volume <0~1>` | `[fade:秒=0.5]` | 调节在播音轨响度，不重启曲目（进入紧张桥段压低 BGM 等）。 |
| `sfx <引用>` | `[from:秒=0]` `[volume:0~1=1.0]` `[loop:bool=false]` | 音效（播放器池，空闲优先分配，可重叠）；`loop:true` 用于环境音。 |
| `sfx stop` | `[引用=全部]` `[fade:秒=0.3]` | 停止指定音效（淡出）；省略引用则停止全部 SFX。 |
| `sfx volume <引用> <0~1>` | `[fade:秒=0.3]` | 调节在播指定音效的响度，不中断播放（环境音渐强渐弱）。 |
| `voice <引用>` | `[from:秒=0]` `[volume:0~1=1.0]` | 语音（单播放器，推进对话自动停止）。 |
| `voice stop` | `[fade:秒=0.1]` | 停止当前语音。 |

- `volume` 为线性 0~1（与设置界面滑条同口径），表示曲目自身响度，与设置里的总线音量正交相乘。
- 保留子动作名：`music` 为 `stop`/`pause`/`resume`/`volume`，`sfx` 为 `stop`/`volume`，`voice` 为 `stop`/`volume`；资源引用名请避开这些词。
- 语音不设逐句调音（`voice volume` 编译期报错）：逐句响度统一属制作期职责，应在音频管线做响度归一；全局调节用设置里的语音总线。设计规则详见 `docs/未来开发设计.md` 阶段七。
- 同曲重发 `music <同一引用>` 不应用新参数（同曲守卫）；调在播曲目响度用 `music volume`。
- 同一音频引用不要以不同 `loop` 语义并发使用（如 `music x loop:true` 与 `sfx x` 同发）——流资源是共享缓存，loop 标志会互踩；`loop:` 对 `AudioStreamInteractive`/`AudioStreamRandomizer` 等复合流无效（循环由资源自身控制）。
- 需要「前奏 → 循环段」式无缝 BGM 时，使用 Godot 的 `AudioStreamInteractive` 资源（经 `index.json` 登记后按普通引用播放），无需 DSL 介入。
- 跳幕/回主菜单会停止全部音频（含 loop 环境音）；读档重放经指令流重建环境音，不叠加。

### 背景

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `bg <引用>` | `[time:秒=0]` | 替换背景；`time > 0` 时交叉淡变。 |

### 立绘

立绘指令直挂实例：`<实例>` 为索引（上限由 `config.json` 的 `character.max` 决定；索引越界含负数在编译期即报错）。每条指令向该实例的动画队列追加一步并自动播放——同实例步骤排队执行，跨实例连写天然并行。`wait:true` 挂起剧情推进直到该实例动画完成（SKIP/读档重放时自动短路为不等待）。

| 指令 | 参数（默认值） | 说明 |
| --- | --- | --- |
| `char <实例> setup <立绘> <x,y>` | | 设置纹理与位置（队列第一步）。 |
| `char <实例> show` | `[time:秒=1.0]` `[wait:bool=false]` | 淡入。 |
| `char <实例> hide` | `[time:秒=1.0]` `[wait:bool=false]` | 淡出。 |
| `char <实例> move <x,y>` | `[time:秒=1.0]` `[wait:bool=false]` | 移动。 |
| `char <实例> texture <立绘>` | | 更换纹理（不改位置）。 |
| `char <实例> wait <秒>` | | 队列内等待（仅正秒）。 |

立绘以**底部中心**为锚点，坐标为**归一化屏幕坐标**（`0.0`–`1.0`，`(0.5, 1.0)` 即屏幕底部中央）。

### 变量

```text
var affection = 3
var affection += 1.5
var affection -= 1
var affection *= 2
var affection /= 2
var luck = random 0 100
```

- 变量全部为**浮点数**（`Global.vars`）；读取未定义变量按 `0.0`。
- 右值可为数字字面量或变量名（`var a = b` 合法）；变量名必须是合法标识符（字母/下划线开头），且不得为条件保留字 `and`/`or`/`not`；右值非法（如 `1x`）同样在编译期报错。
- **随机种子随存档保存**：读档重放前复位种子，重放的随机序列与原游玩一致（确定性重放）。

### 条件分支

```text
if affection >= 3 and not bad_end
	引路人: 你似乎已经很熟悉这里了。
elif (affection > luck or debug_mode) and route < 2
	引路人: 我们见过几面。
else
	引路人: 初次见面。
endif  ← 不存在！缩进回退即结束
```

- 比较符：`==` `!=` `>` `>=` `<` `<=`（`==`/`!=` 为浮点近似比较）；操作数仅为**变量名或数字字面量**（无算术运算——需要计算请先用 `var` 指令算好再比较）。
- 逻辑运算：`and`、`or`、`not`，优先级 `not` > `and` > `or`，可用括号覆盖（v2.3 起）。
- 裸变量真值：`if flag` 等价于 `flag != 0`（v2.3 起）。
- 比较式左值必须是变量名——字面量误写左值（如 `if 3 > 2`）在编译期报错，而非被运行时按未定义变量静默按 0 比较。变量名不得使用条件保留字 `and`/`or`/`not`。
- 支持嵌套（`if` 套 `if`、块内套选项等）。

### 选项

```text
* 前往第二幕
	引路人: 好，我们跳转过去。
	jump demo_scene2
* 留在这里 if:affection >= 3
	引路人: 那我们直接进尾声。
```

- 选项组 = 连续同级 `*` 行（空行/注释不打断）。
- 选项文本免引号直到行尾；`if:` 条件后缀按行尾方向最后一个「 if:」（前有空格）切分。文本确需包含该字样时用双引号包裹文本。
- 选项文本与对话同口径：支持 `{var}` 插值与 `\[`/`\{`/`\\` 转义（经 `DialogueRenderer.render_plain`）；方括号内容原样保留（选项无打字机时间轴，锚点不适用）。
- `if:` 条件与 `if` 指令同语法（比较/逻辑运算/括号/裸变量真值均可用）。
- 条件为假的选项不显示；**整组条件均不满足时跳过该选项组**（警告日志）。
- 选项组**不支持嵌套**（编译期报错）。
- 选项 UI 在上句对话打字完成后立即弹出。

### 跳转与流程

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `jump <剧本名>` | | 切换到另一个剧本（转场、卸载、从头执行）。 |
| `jump main_menu` | | 返回主菜单（特殊目标值）。 |
| `begin <剧本名>` | | 设置下次启动的起始幕（写入 `begin_script` 配置）。 |
| `wait <秒>` | | 剧情显式等待（SKIP/读档重放时短路）；玩家点击可打断。 |

`jump`/`begin` 的目标剧本名在编译期对照剧本目录校验，不存在时给出**警告**而不是错误——目标可能由模组 PCK 提供（纯运行模式的模组包不参与编译，见 `docs/未来开发设计.md` 模组规范）。

### 场景挂载与转场

| 指令 | 参数（默认值） | 说明 |
| --- | --- | --- |
| `scene mount <类型> <名称> <路径>` | `[time:秒=0]` `[anim:动画=fade]` | 挂载场景（类型 `world2d`/`ui`）；`time > 0` 淡入。 |
| `scene unmount <类型> <名称>` | `[time:秒=0]` `[anim:动画=fade]` `[free:bool=true]` | 卸载场景；`time > 0` 先淡出再卸载；`free:false` 回池复用。 |
| `trans in` / `trans out` | `[time:秒=1.0]` `[anim:动画=fade_in/fade_out]` `[wait:bool=true]` | 全屏转场；`wait:true` 挂起剧情直到转场完成（SKIP/重放时短路）。 |

## 语句组合规则

| 容器 \ 语句 | 对话 | 演出指令 | var | jump/begin | if 块 | 选项块 |
| --- | --- | --- | --- | --- | --- | --- |
| 顶层 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 选项分支体 | ✓ | ✓ | ✓ | ✓ | ✓ | ✗（嵌套报错） |
| if 分支体 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 锚点 `[...]` | — | 白名单子集 + pause | ✗ | ✗ | ✗ | ✗ |
| `<`/`>` 前缀修饰 | — | ✓ | ✓ | ✓ | ✗ | ✗ |

补充规则：

- `jump`/`jump main_menu` 之后同块的后续内容**不可达**（跳转即卸载当前剧本），编译期警告一次。
- `<`/`>` 前缀修饰 `if`/`elif`/`else`/`*` 会在编译期报错（结构语句不是指令，时机由块语义决定）。
- 分支体内 `jump` 合法且常见（分支结局跳幕）；该块其余内容视为不可达。

## 资源寻址

指令中的 `<引用>` 按以下顺序解析（`ResourceManager._resolve_path`）：

1. 以 `res://` 或 `user://` 开头：作为 Godot 路径直接使用。
2. 命中 `index.json` 中对应类型的键：用映射路径。
3. 按类型默认目录推导（`audio_dir` / `image_dir`）。
4. 以上都不命中：把原字符串当作路径直接尝试。

推荐在 `index.json` 中为资源登记简短引用名；引用名避免与保留字冲突。

## 完整示例

见 `scripts/demo/scene1.txt` 与 `scripts/demo/scene2.txt`（v2 语法）。

## v1 → v2 迁移对照

| v1 | v2 | 说明 |
| --- | --- | --- |
| `< bg set x` | `bg x`（独立指令）或 `< bg x`（前指令） | 动词 `set` 省略；前缀按绑定方向保留 |
| `< music play x 0 true` | `music x from:0 loop:true` | 键值参数 |
| `< music stop` | `music stop` | 子动作保留 |
| `< voice play x` | `voice x` | 动词省略 |
| `< sfx play x` | `sfx x` | 同上 |
| `< character 0` + `< char setup ...` + `< char play 0` | `char 0 setup ...`（单指令入队自动播放） | 收集模式废除 |
| `< char wait -1` | 废除 | 负秒暂停语义移除 |
| `> voice event 2.5` + `> bg set x` | `台词[bg x]台词` 内联锚点 | 语音事件指令废除，锚定文本位置 |
| `< option "文本"` + `< option end` | `* 文本` + 缩进块 | 缩进回退即结束 |
| `< if x >= 1` ... `< endif` | `if x >= 1` + 缩进块 | 同上 |
| `< var set x 3`（未实现） | `var x = 3` | 操作符语法，已落地 |
| `< begin script x` | `begin x` | 单词化 |
| `< jump main_menu` | `jump main_menu`（`jump` 的特殊目标值） | 收编，不再是独立指令键 |
| 旧存档 | 拒绝载入（版本校验，version < 2） | idx 语义变更不迁移 |
