# GalSGrammar —— BGalS 剧本脚本语言规范

**BGalS（Blessing Galgame Script）** 是 Blessing Galgame Engine（布雷辛格混合视觉小说引擎）的剧本脚本语言。设计初衷是让剧本编写规范化、标准化，并与引擎实现彻底解耦：编剧只需编写纯文本剧本、配置资源索引，无需接触引擎代码即可运行一部视觉小说。

> 本文档以代码实现为准，逐条核对自：
> `Autoloads/dialogue_importer.gd`（解析）、`Scenes/GalEventItem/instruction.gd`（指令枚举与参数）、`Autoloads/story_manager.gd`（运行时）。
> 若文档与代码冲突，以代码为准。

## 设计理念

### 对话块与对话窗口

视觉小说的核心是对话文本：每次对话显示对应一次用户交互，各条指令在对话显示之间执行。因此 BGalS 的流式执行以对话为中心——显示一句对话时，先执行需要在对话显示**之前**执行的**前指令**，然后显示对话，最后执行需要在对话显示**之后**执行的**后指令**。一个完整流程包含的全部内容称为一个**对话块**，一次对话块的执行时空称为**对话窗口**。

```text
[前指令序列] → [对话显示] → [后指令序列] → [下一个对话块]
```

### 流式执行

剧本被编译为事件项序列（`GalEventItemSequence`），运行时按索引顺序逐条解释执行，无需一次性载入全部剧情状态。对话、动画、选项等待都通过 Godot 协程（`await` + 信号）自然挂起与恢复。

### 剧本与引擎解耦

剧本文本不直接操作引擎节点：资源一律通过**路径或引用名**（见 `index.json`）间接寻址，剧本可以被替换、重排、扩展，而引擎代码保持不变。

## 剧本的编译与加载

- 文本剧本放在 `config.json` 中 `scripts.read_dir` 指定的目录（默认 `scripts/`），引擎启动时由 `DialogueImporter` 编译为 Godot 资源，输出到 `scripts.save_dir`（默认 `GalSs/`）。
- 输出文件名为**相对路径去掉扩展名、路径分隔符替换为下划线**：`scripts/demo/scene1.txt` → `GalSs/demo_scene1.tres`，其**脚本名**为 `demo_scene1`。`jump` 等指令引用的就是这个名字。
- 编译按 SHA256 哈希增量进行：只有内容变化的剧本会重新编译（哈希记录于 `GalSs/.hash`）。
- 将 `config.json` 中 `scripts.check` 置为 `false` 可关闭启动时的检查与编译（例如纯运行模组包时）。

## 行规则

BGalS 逐行解析，一行一个语句。每行先做两端空白修剪（缩进没有语法意义），然后：

1. **空行**、以 `//` 或 `#` 开头的行：整行忽略（注释）。
2. 以 `<` 开头的行：**前指令**（在接下来那句对话显示之前执行）。
3. 以 `>` 开头的行：**后指令**（在上那句对话显示之后执行）。
4. 其余行：**对话行**。

## 对话行

```text
说话者: 对话内容
```

- 半角冒号 `:` 与全角冒号 `：` 都作为分隔符，按**第一个**冒号切分：冒号前为说话者，冒号后为内容（内容中后续的冒号原样保留）。
- 不含任何冒号的行视为**旁白**（无说话者）。
- **注意：旁白行不能包含任何冒号**（半角全角都不行），否则冒号前的文字会被当成说话者。
- 对话正文经 `RichTextLabel` 显示，**支持 BBCode 标签**（如 `[b]`、`[color=red]`）；打字机效果会把 `[...]` 标签整体瞬时插入，不会逐字拆坏标签。用反斜杠可转义下一个字符，例如 `\[` 输出字面量 `[`。

```text
讲述者: 你好，欢迎来到 Blessing Galgame Engine。
这是一行旁白。
讲述者: 正文支持 [b]BBCode[/b] 与转义 \[。
```

## 指令行

指令行为 CLI 风格：

```text
< 指令键 参数1 参数2 "含 空格 的 参数3"   // 行内注释
```

- `<` / `>` 之后用空格或制表符分词；**双引号**可包裹含空格的参数（引号本身不进入参数值）。
- 指令行内出现 `//` 时，其后内容被截断（仅指令行有效；对话行中的 `//` 是普通文本）。
- 匹配时**双词指令键优先于单词指令键**（例如 `jump main_menu` 是独立指令键，而不是 `jump` 加参数）。
- 指令键未定义时：输出警告日志并忽略该行（等同于空行）。
- 参数缺失时按下文各指令的默认值补齐；无法转换为数字的参数按 `0.0` 处理。
- 布尔参数：`true` / `1` / `on`（不区分大小写）为真，其余为假。
- 除特殊说明外，同一指令既可作前指令也可作后指令，区别只在执行时机。

## 执行模型速览

运行时以三种迭代模式推进（`StoryManager.IterateMode`）：

| 模式 | 行为 |
| --- | --- |
| `INITIALIZATION` | 剧本载入后的初始阶段：只执行前指令；遇到第一个非前指令项即转入 `DEFAULT` 并继续。用于让开场布置（背景、音乐、初始立绘）在首句对话前完成。 |
| `DEFAULT` | 默认模式：执行指令；遇到对话则显示（等待打字完成），随后转入 `POST_COMMAND`。 |
| `POST_COMMAND` | 只连续执行后指令；遇到非后指令项时本轮执行结束，回到 `DEFAULT`，等待下一次推进。 |

管理维度另有 `ManagerMode`：`INTERACT`（点击推进）、`AUTO`（自动）、`SKIP`（跳过）、`STOP`（屏蔽输入）。细节见 `docs/项目分析.md`。

## 指令参考

### 音频指令

| 指令 | 参数（默认值） | 说明 |
| --- | --- | --- |
| `music play` | `<路径/引用名>` `[起始秒=0]` `[循环=true]` | 播放背景音乐（双播放器交叉淡变，淡入淡出各约 1 秒）。第二参数是**起始秒**，不是音量。 |
| `music pause` | 无 | 暂停当前音乐。 |
| `music resume` | 无 | 恢复暂停的音乐。 |
| `music stop` | 无 | 停止当前音乐（带短淡出）。 |
| `voice play` | `<路径/引用名>` `[起始秒=0]` | 播放角色语音。 |
| `sfx play` | `<路径/引用名>` `[起始秒=0]` | 播放音效（播放器池，可重叠）。 |

```text
< music play demo_bgm 0 true
> sfx play demo_sfx
```

### 语音事件指令

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `voice event` | `<秒>` | 把**紧随其后的一条指令**绑定到语音播放到该时刻触发。 |

- `voice event` 的下一行必须是指令行，该指令不立即执行，而是等语音播放到指定秒数时触发。
- 事件必须在语音**开始播放之前**注册（即 `voice event` 及其绑定的指令要排在对应的 `voice play` 之前）；语音播放中不能再动态添加事件。
- 每次推进对话时运行时会停止语音并清空事件队列；语音被停止（含跳过）时，所有未触发的事件会立即补执行，保证状态不落空。

```text
// 语音播到 2.5 秒时切换背景
> voice event 2.5
> bg set demo_stage_night
> voice play demo_voice
```

### 背景指令

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `bg set` | `<路径/引用名>` | 立即替换背景纹理。 |

### 立绘动画组

引擎内置固定数量的角色实例（上限由 `config.json` 的 `character.max` 决定，默认 4），实例索引从 0 开始。立绘以**底部中心**为锚点，坐标为**归一化屏幕坐标**（`0.0`–`1.0`，`(0.5, 1.0)` 即屏幕底部中央；超出范围的值会被钳制）。

动画采用「先收集、后播放」模型：

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `character` | `<实例索引>` | 开始为该实例收集动画步骤。其后**连续的** `char` 系列指令被收入该实例的动画列表，遇到非 `char` 指令自动结束收集。 |
| `char setup` | `<立绘> <x> <y>` | 设置立绘纹理与位置（作为动画序列的一步，`char play` 播放时生效）。 |
| `char show` | `<秒>` | 淡入显示。 |
| `char hide` | `<秒>` | 淡出隐藏。 |
| `char move` | `<x> <y> <秒>` | 在指定时间内移动到目标位置。 |
| `char wait` | `<秒>` | 等待；秒数为负时暂停序列，直到下一次 `char play`。 |
| `char texture` | `<立绘>` | 更换立绘纹理（不改位置）。 |
| `char play` | `<实例索引>` | 播放该实例已收集的动画列表（播完自动清空）。 |

```text
< character 0
< char setup demo_char_a 0.5 1.0
< char show 0.6
< char move 0.3 1.0 1.0
< char play 0
```

注意事项：

- **动画组内不要夹对话行**：收集循环遇到对话行会将其静默吞掉（既不收入动画也不显示），遇到其他指令则结束收集。
- 动画异步播放，不阻塞对话推进；每次推进对话时所有角色会跳过未完成动画、立即应用最终状态（`skip_all`），因此快进/跳过模式下画面状态仍然正确。
- `character` 与 `char play` 的参数是实例索引，二者需对应。

### 选项指令

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `option` | `<选项文本>` | 声明一个选项；其后紧跟该分支的内容（内联书写）。多个 `option` 依次排列。文本含空格时用双引号包裹。 |
| `option end` | 无 | 收尾一组选项。 |

执行语义：

- 运行时遇到第一个 `option` 时向后扫描到 `option end`，收集全部选项文本并弹出选择 UI。
- 玩家选择后，跳到**所选 `option` 行之后**继续执行。
- 执行某分支时遇到兄弟 `option`，自动跳到 `option end`，不会误入其他分支。
- 选项组**不支持嵌套**（扫描在第一个 `option end` 处截止）。

```text
< option "前往第二幕"
讲述者: 好，我们跳转过去。
< jump demo_scene2

< option "留在这里"
讲述者: 那我们直接进入尾声。

< option end

讲述者: 这里是选项之后的共通部分。
```

### 跳转指令

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `jump` | `<脚本名>` | 切换到另一个剧本：淡出、卸载当前场景与 UI、清空资源缓存、载入目标剧本从头执行。脚本名即编译产物资源名（如 `demo_scene2`）。 |
| `jump main_menu` | 无 | 返回主菜单。 |
| `begin script` | `<脚本名>` | 设置下次启动游戏时的起始幕（写入 `begin_script` 配置，退出游戏时随配置落盘）。 |

### 条件分支指令

| 指令 | 参数 | 说明 |
| --- | --- | --- |
| `if` | `<变量> <比较符> <值>` | 条件为真则进入分支，否则跳到下一个 `elif`/`else`/`endif`。 |
| `elif` | `<变量> <比较符> <值>` | 前置分支均未执行时，再判断本条件。 |
| `else` | 无 | 以上分支均未执行时进入。 |
| `endif` | 无 | 条件结构收尾。支持嵌套。 |

- 剧本变量全部为**浮点数**（`Global.vars: Dictionary[String, float]`）；读取未定义的变量按 `0.0` 处理。
- 比较符支持：`==`、`=`、`!=`、`<>`、`>`、`>=`、`<`、`<=`；无法识别的比较符按 `==` 处理并输出警告。
- 条件结构本身已可用，但**变量写入指令（`var` 系列）尚未接入运行时**（见下节），在 `var` 指令落地前，变量只能由模组代码写入，纯剧本场景下恒为 `0.0`。

```text
< if affection >= 3
讲述者: 你似乎已经很熟悉这里了。
< elif affection > 0
讲述者: 我们见过几面。
< else
讲述者: 初次见面。
< endif
```

### 已注册语法、运行时尚未实现的指令

下列指令已进入解析器（指令键、`Instruction.Head` 枚举与参数转换均已就绪），但**运行时处理函数尚未注册，执行时会被静默跳过**。请勿在正式剧本中依赖它们；实现计划见 [ROADMAP.md](ROADMAP.md)。

| 指令 | 参数（默认值） | 预定语义 |
| --- | --- | --- |
| `var set` | `<变量> <值>` | 变量赋值。 |
| `var add` | `<变量> <值>` | 变量加算。 |
| `var sub` | `<变量> <值>` | 变量减算。 |
| `var mul` | `<变量> <值>` | 变量乘算。 |
| `var div` | `<变量> <值>` | 变量除算。 |
| `var random` | `<变量> [最小=0.0] [最大=1.0]` | 变量取随机数。 |
| `scene mount` | `<类型=world2d> <名称=default> <路径> [时长=0] [动画=fade]` | 剧本直接挂载场景。 |
| `scene unmount` | `<类型=world2d> <名称=default> [时长=0] [动画=fade] [释放=true]` | 剧本直接卸载场景。 |
| `trans in` | `[时长=1.0] [动画=fade_in] [等待=true]` | 剧本直接触发淡入转场。 |
| `trans out` | `[时长=1.0] [动画=fade_out] [等待=true]` | 剧本直接触发淡出转场。 |

## 资源寻址

指令中的 `<路径/引用名>` 按以下顺序解析（`ResourceManager._resolve_path`）：

1. 以 `res://` 或 `user://` 开头：作为 Godot 路径直接使用。
2. 命中 `index.json` 中对应类型的键：用映射路径。
3. 按类型默认目录推导（`audio_dir` / `image_dir`）。
4. 以上都不命中：把原字符串当作路径直接尝试。

推荐在 `index.json` 中为资源登记简短引用名，剧本里只写引用名，做到素材替换不改剧本。

## 完整示例

以下示例使用仓库自带演示资源（引用名见 `index.json`），风格与 `scripts/demo/` 一致：

```text
// ============================================
// BGalS 完整示例
// ============================================

// 开场布置（前指令）
< bg set demo_stage
< music play demo_bgm 0 true

// 立绘动画组：0 号实例淡入
< character 0
< char setup demo_char_a 0.5 1.0
< char show 0.6
< char play 0

讲述者: 欢迎使用 Blessing Galgame Engine。
这是旁白——不含冒号的行会被视为旁白。

// 后指令：这句对话显示之后切换背景
讲述者: 这句话说完，背景会切换。
> bg set demo_stage_night

// 立绘移动到底部偏左
< character 0
< char move 0.3 1.0 1.0
< char play 0

讲述者: 立绘可以移动、换图、淡入淡出。

// 选项分支
< option "前往第二幕"
讲述者: 好，我们跳转过去。
< jump demo_scene2

< option "留在这里"
讲述者: 那我们直接进入尾声。

< option end

// 条件分支（变量未定义时按 0.0 处理）
< if affection >= 3
讲述者: 你似乎已经很熟悉这里了。
< else
讲述者: 初次见面，请多关照。
< endif

// 收尾：把起始幕设回演示 1，然后返回主菜单
讲述者: 演示到此结束。
< begin script demo_scene1
< jump main_menu
```
