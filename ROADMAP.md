# ROADMAP —— Blessing Galgame Engine 开发路线图

本文把 `docs/未来开发设计.md` 中的设想整理为正式路线图，每条给出**目标、动机、现状、落地要点**。排序不代表严格优先级，但大体按「先补齐已承诺的能力，再扩展新能力」组织。

## R0. 已注册待实现的指令

**目标**：让 `var` 系列、`scene mount/unmount`、`trans in/out` 从「语法可用」变为「运行时可用」。

**动机**：这些指令键已进入解析器（`DialogueImporter.INSTRUCTION_MAP`）、`Instruction.Head` 枚举和参数转换都已就绪，但 `StoryManager._instruction_handlers` 未注册对应处理函数——剧本写了会被**静默跳过**，是编剧最容易踩的坑。

**现状**：

| 指令 | 解析器 | 参数转换 | 运行时 handler |
| --- | --- | --- | --- |
| `var set/add/sub/mul/div` | ✅ | ✅（key + float） | ❌ |
| `var random` | ✅ | ✅（key + min + max） | ❌ |
| `scene mount` | ✅ | ✅（type/name/path/duration/anim） | ❌ |
| `scene unmount` | ✅ | ✅（type/name/duration/anim/free） | ❌ |
| `trans in` / `trans out` | ✅ | ✅（duration/anim/wait） | ❌ |

**落地要点**：

- `var` 系列 handler 统一读写 `Global.vars`，未定义变量按 `0.0` 处理，与条件分支的读取语义保持一致。
- `scene` 系列直接转发 `SceneManager`，不新增平行场景系统；为可被剧本挂载的场景建立命名规范或白名单，并区分常驻/临时场景的 `free` 策略。
- `trans` 系列复用 `SceneManager.transition()`。
- 每落地一条，同步更新 `GalSGrammar.md` 的指令表并把该行从本节移除（见 R1）。

## R1. 指令能力对照表治理

**目标**：保证「文本语法（`INSTRUCTION_MAP`）— 指令枚举（`Instruction.Head`）— 运行时（`_instruction_handlers`）」三处长期同步。

**动机**：指令能力分布在三处手工维护的代码里，R0 的缺口正是漂移的实例；新贡献者加指令时容易只改其中一两处。

**现状**：`GalSGrammar.md` 的指令表即当前事实上的对照表（含每条指令的实现状态）；R0 一节列出所有漂移项。

**落地要点**：

- 新增指令的 PR 必须同时改齐三处并更新 `GalSGrammar.md`。
- 可在导入期或测试脚本中自动比对 `Instruction.Head` 与 `_instruction_handlers` 的键集，列出未实现项，防止再次漂移。

## R2. 叙事图构建

**目标**：以**节点 = 剧本（幕）、边 = `jump` / 选项分支 / 条件分支**构建作品级叙事图，提供不可达节点检测与流程图导出；长期将 `StoryManager` 重构为「**从叙事图构建的状态机**」。

**动机**：剧本数量与分支增多后，纯文本难以总览作品结构；不可达幕、死循环跳转等问题只能靠人肉排查。

**现状**：`jump`、`begin script`、`option`、`if/elif/else/endif` 均已实现，剧本间跳转关系存在但无全局视图；运行时仍是「单序列 + 索引跳转」模型。

**落地要点**：

- 第一步是静态分析工具而非改运行时：在导入期扫描全部剧本，提取跳转/选项/条件边，输出图数据。
- 在图数据上做不可达节点检测、环检测，并导出 Mermaid/Graphviz 流程图供编剧审阅。
- 图模型稳定后，再评估 `StoryManager` 重构：把「当前幕 + 行索引」的执行模型抽象为「叙事图上的状态机」，存档格式随之演进（见 R5）。

## R3. 场景缓冲池演进

**目标**：在现有 `SceneManager` pool/mounted 双结构基础上，演进为策略更明确的场景缓冲池。

**动机**：场景数量增长后，「一律预加载常驻」与「用完即释放」两个极端都不合适，需要按场景类型分层管理内存与加载耗时。

**现状**：`world2d` / `ui` 两通道，支持 `pre_load` / `mount` / `unmount(free)` / `umount_all` / `free_scenes`；核心 UI 采用卸载不释放的常驻策略，切换零磁盘 I/O。

**落地要点**：

- 按类型/章节制定预加载与驻留策略（如：UI 常驻、背景与立绘按章节预热、临时场景用完释放）。
- 先采集实际加载耗时与内存占用，用数据驱动分层，不提前复杂化。
- 与 R0 的 `scene mount/unmount` 指令落地协同：剧本可控的挂载必须服从同一套池策略。

## R4. 变量系统

**目标**：确立剧本变量的完整语义，并随 R0 的 `var` 指令落地生效。

**动机**：条件分支已经可用，但变量写入指令缺位，纯剧本场景下变量恒为 `0.0`；语义需在落地前敲定，避免事后返工。

**现状**：`Global.vars` 为 `Dictionary[String, float]`——**全浮点约定**；读取未定义变量按 `0.0`；变量表随存档保存并在读档时恢复。

**落地要点**：

- 维持全浮点约定，不引入多类型变量，保持条件比较与存档格式简单。
- `var random` 需要明确**随机种子是否入档**的取舍：
  - 种子入档：读档后的 SKIP 重放完全确定，与重放式存档的确定性假设一致，但实现更重。
  - 种子不入档：实现简单，但读档重放经过随机点时可能与原游玩过程分叉，导致后续条件分支走向不同。
  - 建议：默认种子入档并在 `load_game` 时复位；若未来允许「非确定性重放」，需在文档中显式声明该风险。

## R5. 存档版本迁移策略

**目标**：为 `SavedGame` 增加版本字段与迁移机制，保证旧存档在引擎升级后仍可用。

**动机**：存档字段将持续扩展（历史日志、预览信息、叙事图位置等），没有版本策略时，每次改动都在透支玩家存档的兼容性。

**现状**：`SavedGame` 保存 `vars` / `script_name` / `idx` / `execution_stack` / `timestamp` / `title` / `thumbnail_path`，无版本字段；读档依赖 SKIP 重放恢复现场。

**落地要点**：

- 增加 `@export var version: int = 1`；旧存档缺字段时按版本 1 处理，新字段一律提供默认值。
- 重大结构变化（如 R2 叙事图改造）配套迁移函数。
- 原则：能由剧本重放推导的状态不入档，只保存不可推导的最小集合。

## R6. 历史回看与回滚设想

**目标**：先实现**历史回看**（只读对话日志），再评估**回滚**（回到此前任意对话点）。

**动机**：两者都是视觉小说的标配体验；回滚会显著改变存档与重放机制，不能贸然开工。

**现状**：无存档之外的对话日志；SKIP 重放机制已证明「从剧本重建现场」可行，是回滚的技术基础。

**落地要点**：

- 历史回看：每次显示对话时记录一条结构化日志（`script_name` / `idx` / 说话者 / 文本 / 时间戳），配独立 HistoryUI，打开时暂停剧情、关闭后恢复。
- 回滚：需要更完整的状态点（变量快照、背景、立绘纹理/位置/透明度、当前音乐与播放位置、条件执行栈），并与 R5 的存档格式、R4 的随机种子策略联动设计。
- 建议顺序：历史回看先行，回滚待其稳定后再立项评估。

---

更完整的设计背景与原则见 [docs/未来开发设计.md](docs/未来开发设计.md)。
